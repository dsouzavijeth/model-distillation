[project]
name = "modeldistillation"
version = "0.1.0"
description = "Student-Teacher Model Distillation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "modal>=0.73.0",
    "python-dotenv>=1.2.2",
]